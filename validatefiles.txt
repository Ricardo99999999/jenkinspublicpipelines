pipeline {
  agent any
  stages {
  
    stage('Checking versions') {
      steps {
          script{
            def nodeVersion = bat(script: 'node -v', returnStdout: true).trim()
            def npmVersion  = bat(script: 'npm -v', returnStdout: true).trim()
            echo "La versión de Node es: ${nodeVersion}"
            echo "La versión de NPM es: ${npmVersion}"
            //The same as
            //bat 'node -v'
            //bat 'npm -v'
          }
      }
    }
	
    stage('Cloning GitHub repository') {
      steps {
         // Clone GitHub repository
        git branch: 'master', url: 'https://github.com/Ricardo99999999/validatefiles.git'
      }
    }
	
	stage('Install dependencies') {
      steps {
        bat 'npm install'
      }
    }
	
	stage('Run validate') {
      steps {
        bat 'npm run validate'
      }
    }
  }
}
